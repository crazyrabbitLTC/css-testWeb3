import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Center({}: Props): ReactElement {
    return (
        <div>
            Center Things
        </div>
    )
}
